<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:WarHub.Armoury.GodMode.Modules.DataAccess.Views;assembly=WarHub.Armoury.GodMode"
             x:Class="WarHub.Armoury.GodMode.Modules.DataAccess.Views.CatalogueDetailsPage"
             x:Name="Page"
             BindingContext="{x:Static views:ViewModelLocator.CatalogueDetailsViewModel}"
             Title="Catalogue details">

    <Grid>

        <StackLayout Spacing="15">

            <Label Text="{Binding CatalogueInfo.Name}" FontAttributes="Bold" FontSize="24" />

            <Label Text="{Binding CatalogueInfo.Revision, StringFormat='v{0}'}" />

            <Label Text="{Binding CatalogueInfo.AuthorName, StringFormat='by {0}'}" />

            <Label Text="{Binding CatalogueInfo.RawId, StringFormat='id: {0}'}" />

            <Label Text="{Binding CatalogueInfo.GameSystemRawId, StringFormat='game system id: {0}'}" />

            <Label Text="{Binding CatalogueInfo.Sourcebook, StringFormat='books: {0}'}" />

            <Label Text="{Binding CatalogueInfo.OriginProgramVersion, StringFormat='BattleScribe version: {0}'}" />

            <Button Text="Open in editor" Command="{Binding OpenCatalogueCommand}"
                    CommandParameter="{Binding CatalogueInfo}" />

        </StackLayout>

    </Grid>

</ContentPage>