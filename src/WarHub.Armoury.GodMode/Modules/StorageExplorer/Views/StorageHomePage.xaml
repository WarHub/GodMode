<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WarHub.Armoury.GodMode.Modules.StorageExplorer.Views.StorageHomePage"
             xmlns:local="clr-namespace:WarHub.Armoury.GodMode.Modules.StorageExplorer.Views;assembly=WarHub.Armoury.GodMode"
             BindingContext="{x:Static local:ViewModelLocator.StorageHomeViewModel}"
             Title="Browse storage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Text="Local Storage" Command="{Binding OpenFolderCommand}"
                CommandParameter="{Binding LocalFolder}" />
        <Button Grid.Row="1" Text="Roaming Storage" Command="{Binding OpenFolderCommand}"
                CommandParameter="{Binding RoamingFolder}" />
    </Grid>
</ContentPage>